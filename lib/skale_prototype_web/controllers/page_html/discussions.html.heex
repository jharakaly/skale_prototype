<div class="container mx-auto px-4 pt-1">
  <!-- What's on your mind + Search row -->
  <div class="search-row mb-0 flex flex-col md:flex-row gap-3">
    <!-- What's on your mind (60%) -->
    <div class="w-full md:w-[60%]">
      <div class="whats-on-mind-bar" id="whats-on-mind-bar" phx-click="open_new_post_modal" style="margin-bottom: 0.25rem;">
        <div class="profile-avatar">
          <div class="avatar-initials">Y</div>
        </div>
        <div class="whats-on-mind-input">
          <input
            type="text"
            placeholder="What's on your mind?"
            class="whats-on-mind-input-field"
            readonly
          />
        </div>
      </div>
    </div>

    <!-- Search bar (30%) - FIXED TO MATCH BUILDING COMMUNITY PAGE -->
    <div class="w-full md:w-[30%]">
      <div class="left-search-container">
        <span class="search-icon">🔍</span>
        <input
          type="text"
          placeholder="Search discussions..."
          class="search-input"
        />
      </div>
    </div>
  </div>

  <!-- Breadcrumb with fucking reduced spacing -->
  <div class="breadcrumb flex items-center gap-2 text-sm text-gray-600 mt-0 mb-0">
    <a href="/" class="hover:text-gray-800">Home</a>
    <span>/</span>
    <span class="text-[#A3B18A] font-semibold">Discussions</span>
  </div>

  <!-- Discussions Grid -->
  <div class="discussion-grid grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    <!-- Building Skale -->
    <a href="/group_building_skale" class="discussion-card block rounded-xl border border-gray-200 p-5 shadow-sm hover:shadow-md hover:-translate-y-1 transition">
      <div class="h-40 rounded-md bg-[#A3B18A] mb-4"></div>
      <h3 class="font-semibold text-gray-900 mb-1">Building Skale</h3>
      <p class="text-sm text-gray-600">A group for shaping, developing, and supporting the Skale community — from culture and recruiting to onboarding and platform growth.</p>
    </a>

    <!-- Vegan Recipe Tips - ONLY LINK UPDATED -->
    <a href="/groups" class="discussion-card block rounded-xl border border-gray-200 p-5 shadow-sm hover:shadow-md hover:-translate-y-1 transition">
      <div class="h-40 rounded-md bg-gray-300 mb-4"></div>
      <h3 class="font-semibold text-gray-900 mb-1">Vegan Recipe Tips</h3>
      <p class="text-sm text-gray-600">Share your best vegan cooking techniques and ingredient substitutions.</p>
    </a>

    <div class="discussion-card block rounded-xl border border-gray-200 p-5 shadow-sm">
      <div class="h-40 rounded-md bg-gray-300 mb-4"></div>
      <h3 class="font-semibold text-gray-900 mb-1">Local Farmers Markets</h3>
      <p class="text-sm text-gray-600">Discuss the best local markets and seasonal produce finds.</p>
    </div>

    <div class="discussion-card block rounded-xl border border-gray-200 p-5 shadow-sm">
      <div class="h-40 rounded-md bg-gray-300 mb-4"></div>
      <h3 class="font-semibold text-gray-900 mb-1">Kitchen Equipment</h3>
      <p class="text-sm text-gray-600">Essential tools and gear recommendations for home cooks.</p>
    </div>

    <div class="discussion-card block rounded-xl border border-gray-200 p-5 shadow-sm">
      <div class="h-40 rounded-md bg-gray-300 mb-4"></div>
      <h3 class="font-semibold text-gray-900 mb-1">Meal Prep Ideas</h3>
      <p class="text-sm text-gray-600">Weekly planning and batch cooking strategies.</p>
    </div>

    <div class="discussion-card block rounded-xl border border-gray-200 p-5 shadow-sm">
      <div class="h-40 rounded-md bg-gray-300 mb-4"></div>
      <h3 class="font-semibold text-gray-900 mb-1">Sustainable Cooking</h3>
      <p class="text-sm text-gray-600">Reducing food waste and eco-friendly kitchen practices.</p>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Simple infinite scroll placeholder
    const grid = document.querySelector('.discussion-grid');
    if (grid) {
      const sentinel = document.createElement('div');
      sentinel.id = 'scroll-sentinel';
      grid.after(sentinel);

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            console.log('Loading more discussions...');
          }
        });
      });
      observer.observe(sentinel);
    }

    // "What's on your mind?" bar -> open New Post Modal (same behavior as homepage)
    const whatsOnMindBar = document.getElementById('whats-on-mind-bar');
    if (whatsOnMindBar) {
      whatsOnMindBar.addEventListener('click', function() {
        const newPostModal = document.getElementById('new-post-modal');
        if (newPostModal) {
          newPostModal.classList.remove('hidden');
          document.body.style.overflow = 'hidden';
        } else {
          console.error('New post modal not found in DOM');
        }
      });
    }
  });
</script>

<style>
  /* Match homepage "What's on your mind?" styling */
  .whats-on-mind-bar {
    display: flex;
    align-items: center;
    background-color: white;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    padding: 0.1rem 1rem;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
    cursor: pointer;
    transition: all 0.2s ease;
    width: 100%; /* fills its 60% column */
  }

  .whats-on-mind-bar:hover {
    border-color: #a3b18a;
    box-shadow: 0 2px 4px rgba(163, 177, 138, 0.1);
  }

  .profile-avatar {
    margin-right: 0.75rem;
  }

  .avatar-initials {
    width: 1.7rem;
    height: 1.7rem;
    background-color: #a3b18a;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: bold;
    font-size: 0.8rem;
  }

  .whats-on-mind-input {
    flex: 1;
  }

  .whats-on-mind-input-field {
    width: 100%;
    border: none;
    outline: none;
    font-size: 0.9rem;
    color: #6b7280;
    background: transparent;
    cursor: pointer;
  }

  .whats-on-mind-input-field::placeholder {
    color: #6b7280;
  }

  /* REDUCE SPACING FOR DISCUSSIONS PAGE SPECIFICALLY */
  .container.mx-auto.px-4 {
    padding-top: 0.25rem !important; /* Reduced from 0.75rem to 0.25rem (66% reduction) */
  }
  
  .breadcrumb {
    margin-top: 0 !important;
    margin-bottom: 0 !important;
    padding-top: 0.125rem !important;
    padding-bottom: 0.125rem !important;
  }
</style>